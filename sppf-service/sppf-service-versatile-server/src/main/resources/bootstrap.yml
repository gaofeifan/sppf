eureka:
  client:
    service-url:
      ##defaultZone: http://eureka1:8761/eureka,http://eureka2:8762/eureka,http://eureka3:8763/eureka
      defaultZone:  http://192.168.1.239:8761/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: versatile
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG

security:
  oauth2:
    resource:
      id: resource
      user-info-uri: http://localhost:8086/principal
      prefer-token-info: false

